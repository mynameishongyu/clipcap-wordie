Place committed font files for server-side canvas rendering here.

Recommended file for slot annotation labels:

- `NotoSans-Regular.ttf`

The slot-fill API can register this font on Vercel so bbox labels such as
`0-1-2` render as text instead of solid fallback blocks.
